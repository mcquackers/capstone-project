<%= form_for [user, status_entry] do |form| %>
  <%= form.text_field :body, placeholder: "What's up?" %>
  <%= form.text_field :image_url, placeholder: "Add an image" %>
  <%= form.submit %>
<% end %>
