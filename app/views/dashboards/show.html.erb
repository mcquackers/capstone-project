<h1>Viewing <%= @user.name %>'s Dashboard</h1>
<%= link_to "Marketplace", area_listings_path(current_user.area_id) %>
<%= link_to "Cycling Clubs", area_clubs_path(current_user.area_id) %>
<%= link_to "Notifications", user_notifications_path(@user) %>
<%= link_to "#{@user.area.name}'s Courses", area_courses_path(@user.area) %>
